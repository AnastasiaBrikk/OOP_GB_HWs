## 1 - *srp* - ћного ответственности дл€ одного класса, убрала лишние методы из класса user.
## 2 - *ocp* - ¬ случае если потребует сохранить другой тип данных в другую базу данных придЄтс€ модифицировать класс save. ѕоэтому создаем класс SaveUser, отвечающий за конктреное действие с конкретным типом. ≈сли потребуетс€, можно будет дополнить класс либо создать новый, например, SaveData, который будет работать с другим типом данных или хранилищем. “оже самое и с классом ReportUser.
## 3 - *dip* - Persister сделала независимым от класса User и также сделала из него лист<T>. “еперь в него можно будет сохран€ть любой тип данных. ¬ идеале реализовать абстрактный метод db.add(user) в UserSave, но € не джавист (не получилось).